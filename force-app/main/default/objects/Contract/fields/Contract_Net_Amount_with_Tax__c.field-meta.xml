<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contract_Net_Amount_with_Tax__c</fullName>
    <formula>BLANKVALUE(Rental_Amount__c, 0) + BLANKVALUE(Services_Amount__c, 0) + BLANKVALUE( Overage_Amount__c , 0) + BLANKVALUE(Contract_Tax_Amount__c, 0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Contract Net Amount with Tax</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
